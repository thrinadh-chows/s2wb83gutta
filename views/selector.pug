extends layout

block content
  h1= title
  - var image_names= ["watch.jpg","chocolate.jpg","cycle.jpg","greeting.jpg","rose.jpg"]            
        -for(var i=0;i<3;i++){
            - var number=Math.floor(Math.random()*5)
                img(src= `/images/${image_names[number]}` alt="images", height="300", width="300")
        -}